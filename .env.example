# Copy to .env.local at the venue and fill in real keys.
# With keys absent (or MOCK=1) the app runs the scripted demo pipeline —
# pixel-identical UI, no network needed.
SARVAM_API_KEY=
ANAKIN_API_KEY=
# MOCK=1
